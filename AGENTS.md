# NexaPixel Portfolio

- Use `npm install` for dependencies.
- Run `npm run build` before publishing changes.
- Run `npm run deploy` to deploy the production Worker to Cloudflare.
- Keep `wrangler.jsonc` as the source of truth for Cloudflare configuration.
- Never commit Cloudflare credentials or `.dev.vars`.
